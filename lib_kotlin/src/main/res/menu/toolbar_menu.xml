<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item
        android:id="@+id/backup"
        android:title="Backup"
        android:icon="@drawable/ic_backup"
        app:showAsAction="always"
       />
    <item
        android:id="@+id/delete"
        android:icon="@drawable/ic_delete"
        android:title="Delete"
        app:showAsAction="ifRoom" />
    <!-- 当只设置了 文字时，只显示文字， 文字+图片 在 Toolbar 上只显示 图片-->
    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_settings"
        android:title="Settings"
        app:showAsAction="never" />

   <!-- app:showAsAction="always"   总是显示在 Toolbar 上 -->
   <!-- app:showAsAction="ifRoom"   如果 Toolbar 空间够用则显示 -->
   <!-- app:showAsAction="never"    从不在Toolbar 上显示，只显示在下拉菜单中-->


   <!-- action 按钮 在 Toolbar 上只会显示 图片，在菜单中只显示 文字-->


</menu>
