package com.example.jetpack.livedata;

import androidx.lifecycle.ViewModelProvider;
import androidx.lifecycle.ViewModelProviders;

public class LiveDataMain {
    // LveData提供了两种改变数据的方法：setValue()和postValue()。
    // 区别是setValue()要在主线程中调用，而postValue()既可在主线程也可在子线程中调用。

//   ViewModelProviders
}
